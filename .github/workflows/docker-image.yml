name: Docker-Image erstellen

on:
  workflow_dispatch:
    inputs:
      tag: '3.5.0'

jobs:

  build:

    runs-on: ubuntu-latest

    steps:
    - uses: actions/checkout@v4
    - name: Build the Docker image
      run: docker build . --file Dockerfile --tag oereb_server:${{ inputs.tags }}
